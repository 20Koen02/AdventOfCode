import { challenge } from './Main';

describe('19', () => {
  it('part 1 should equal to 115', async () => {
    expect(challenge.partOne).toEqual('115');
  });

  it('part 2 should equal to 237', async () => {
    expect(challenge.partTwo).toEqual('237');
  });
});
