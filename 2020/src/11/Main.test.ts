import { challenge } from './Main';

describe('11', () => {
  it('part 1 should equal to 2476', async () => {
    expect(challenge.partOne).toEqual('2476');
  });

  it('part 2 should equal to 2257', async () => {
    expect(challenge.partTwo).toEqual('2257');
  });
});
