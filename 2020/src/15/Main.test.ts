import { challenge } from './Main';

describe('15', () => {
  it('part 1 should equal to 387', async () => {
    expect(challenge.partOne).toEqual('387');
  });

  it('part 2 should equal to 6428', async () => {
    expect(challenge.partTwo).toEqual('6428');
  });
});
