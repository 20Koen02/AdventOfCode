import { challenge } from './Main';

describe('12', () => {
  it('part 1 should equal to 1710', async () => {
    expect(challenge.partOne).toEqual('1710');
  });

  it('part 2 should equal to 62045', async () => {
    expect(challenge.partTwo).toEqual('62045');
  });
});
