import { challenge } from './Main';

describe('08', () => {
  it('part 1 should equal to 2003', async () => {
    expect(challenge.partOne).toEqual('2003');
  });

  it('part 2 should equal to 1984', async () => {
    expect(challenge.partTwo).toEqual('1984');
  });
});
