import { challenge } from './Main';

describe('07', () => {
  it('part 1 should equal to 302', async () => {
    expect(challenge.partOne).toEqual('302');
  });

  it('part 2 should equal to 4165', async () => {
    expect(challenge.partTwo).toEqual('4165');
  });
});
