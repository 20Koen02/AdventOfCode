import { challenge } from './Main';

describe('13', () => {
  it('part 1 should equal to 333', async () => {
    expect(challenge.partOne).toEqual('333');
  });

  it('part 2 should equal to 690123192779524', async () => {
    expect(challenge.partTwo).toEqual('690123192779524');
  });
});
