import { challenge } from './Main';

describe('17', () => {
  it('part 1 should equal to 291', async () => {
    expect(challenge.partOne).toEqual('291');
  });

  it('part 2 should equal to 1524', async () => {
    expect(challenge.partTwo).toEqual('1524');
  });
});
