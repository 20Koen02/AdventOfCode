import { challenge } from './Main';

describe('09', () => {
  it('part 1 should equal to 36845998', async () => {
    expect(challenge.partOne).toEqual('36845998');
  });

  it('part 2 should equal to 4830226', async () => {
    expect(challenge.partTwo).toEqual('4830226');
  });
});
