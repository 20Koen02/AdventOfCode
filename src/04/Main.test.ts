import { challenge } from './Main';

describe('04', () => {
  it('part 1 should equal to 208', async () => {
    expect(challenge.partOne).toEqual('208');
  });

  it('part 2 should equal to 167', async () => {
    expect(challenge.partTwo).toEqual('167');
  });
});
