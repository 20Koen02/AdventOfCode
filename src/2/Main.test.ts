import { challenge } from './Main';

describe('template', () => {
  it('part 1 should equal to 398', async () => {
    expect(challenge.partOne).toEqual('398');
  });

  it('part 2 should equal to 562', async () => {
    expect(challenge.partTwo).toEqual('562');
  });
});
