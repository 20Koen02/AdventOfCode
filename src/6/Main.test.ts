import { challenge } from './Main';

describe('6', () => {
  it('part 1 should equal to 6775', async () => {
    expect(challenge.partOne).toEqual('6775');
  });

  it('part 2 should equal to 3356', async () => {
    expect(challenge.partTwo).toEqual('3356');
  });
});
