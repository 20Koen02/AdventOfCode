import { challenge } from './Main';

describe('05', () => {
  it('part 1 should equal to 935', async () => {
    expect(challenge.partOne).toEqual('935');
  });

  it('part 2 should equal to 743', async () => {
    expect(challenge.partTwo).toEqual('743');
  });
});
