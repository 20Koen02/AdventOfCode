import { challenge } from './Main';

describe('01', () => {
  it('part 1 should equal to 806656', async () => {
    expect(challenge.partOne).toEqual('806656');
  });

  it('part 2 should equal to 230608320', async () => {
    expect(challenge.partTwo).toEqual('230608320');
  });
});
